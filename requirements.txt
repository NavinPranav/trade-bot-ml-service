# Core
fastapi==0.111.0
uvicorn[standard]==0.30.1
# Python 3.14: use grpc/protobuf releases with cp314 wheels (avoid grpcio-tools source build).
grpcio==1.80.0
grpcio-tools==1.80.0
protobuf==6.31.1

# Database
sqlalchemy==2.0.30
psycopg[binary]==3.2.13
alembic==1.13.1
redis==5.0.4

# Data (needed for Gemini + gRPC OHLCV helpers; do not remove while AI path is active)
pandas==2.2.2
numpy==1.26.4
ta==0.11.0
yfinance==0.2.50
urllib3>=1.26.18,<2
certifi>=2024.2.2

# --- ML/DL / registry (commented — ensemble + training off; see ML_PIPELINE_ACTIVE in app/inference/predictor.py) ---
# Uncomment when re-enabling the classical ML + stats + DL stack:
# arch==7.0.0
# statsmodels==0.14.2
# xgboost==2.0.3
# lightgbm==4.3.0
# scikit-learn==1.4.2
# optuna==3.6.1
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch==2.3.0+cpu
# pytorch-lightning==2.2.4
# transformers==4.40.2
# celery[redis]==5.4.0
# mlflow==2.17.2

# Utils
python-dotenv==1.0.1
pydantic-settings==2.2.1
httpx==0.27.0
loguru==0.7.2